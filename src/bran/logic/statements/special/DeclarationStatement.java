package bran.logic.statements.special;

public class DeclarationStatement {
	//sets definitions and rules rather than following them
	//get contrapositive / inverse / converse. (do this with .not() statements. define negatives. bran.proofs: even odd have definitions)
}
